<form method="post" action="{{ path('app_grammage_corps_delete', {'id': grammage_corp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grammage_corp.id) }}">
    <button class="btn">Delete</button>
</form>
