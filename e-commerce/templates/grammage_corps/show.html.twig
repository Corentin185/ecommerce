{% extends 'base.html.twig' %}

{% block title %}GrammageCorps{% endblock %}

{% block body %}
    <h1>GrammageCorps</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grammage_corp.id }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ grammage_corp.poids }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_grammage_corps_index') }}">back to list</a>

    <a href="{{ path('app_grammage_corps_edit', {'id': grammage_corp.id}) }}">edit</a>

    {{ include('grammage_corps/_delete_form.html.twig') }}
{% endblock %}
