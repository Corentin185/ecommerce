<form method="post" action="{{ path('app_antenne_couleur_delete', {'id': antenne_couleur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ antenne_couleur.id) }}">
    <button class="btn">Delete</button>
</form>
